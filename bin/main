#!/usr/bin/env node
var program = {
	apps: require('./apps'),
//	jobs: require('./jobs'),
	worker: require('./worker')
}


var args = process.argv;
var name = args[2];

var subcommand = program[name];
args.splice(2,1);
subcommand.parse(args);